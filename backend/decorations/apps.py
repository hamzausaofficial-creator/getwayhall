from django.apps import AppConfig


class DecorationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'decorations'
    verbose_name = '09 — Decoration packages'
