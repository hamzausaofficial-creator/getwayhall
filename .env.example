# Django
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL) — omit or use SQLite by adjusting settings.py for local dev
DB_NAME=hallora
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# Notifications: console (dev) | twilio (live SMS/WhatsApp)
NOTIFICATION_BACKEND=console

# Twilio (when NOTIFICATION_BACKEND=twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
TWILIO_WHATSAPP_FROM=
